[project]
name = "boiling-point-converter"
authors = [
    {name = "James Nobles"},
]
version = "0.1.0"
description = "TUI application for Clausius-Clapeyron boiling point calculations."
requires-python = ">=3.12"
dependencies = ["textual>=0.70"]
license = "MIT"
license-files = ["LICENSE.txt"]
readme = "README.md"
classifiers = [
    "Private :: Do Not Upload",
    "License :: OSI Approved :: MIT License"
]

[project.scripts]
boiling-point-converter = "boiling_point_converter.__main__:main"

[project.urls]
repository = "https://github.com/jnobles/Temperature-Pressure-Calculator"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/boiling_point_converter"]

[tool.ruff.lint]
extend-select = ["I"]

[tool.uv]
python-downloads = "manual"

[tool.pip-licenses]
ignore-packages = ["boiling-point-converter"]
output-file = "THIRD_PARTY_LICENSES.txt"
format = "plain-vertical"
with-authors = true
with-license-file = true
no-license-path = true

[dependency-groups]
dev = [
    "coverage>=7.15.1",
    "hypothesis>=6.156.1",
    "just>=0.8.165",
    "pip-licenses>=5.5.5",
    "pyinstaller>=6.21.0",
    "pytest>=9.1.1",
    "ruff>=0.15.20",
]

[tool.coverage.run]
source = ["boiling_point_converter"]
branch = true

[tool.coverage.report]
show_missing = true
skip_covered = true

[tool.coverage.html]
skip_covered = false
